name: pull request workflow
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Run tests
        run: |
          ls -lart